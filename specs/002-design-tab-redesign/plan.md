# Implementation Plan: Right Panel Design Tab Redesign

**Branch**: `002-design-tab-redesign` | **Date**: 2026-02-14 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/002-design-tab-redesign/spec.md`

## Summary

Redesign the right panel Design tab from a traditional vertical-stacked layout with full-width `PropertyInput` components to a compact Figma-style properties panel. The redesign introduces compact inline inputs, icon toggle groups, linked padding/margin controls, a gradient editor, shadow editor, and a Design/CSS tab toggle. All changes are UI-only — the existing Zustand store, `useChangeTracker` hook, postMessage bridge, and localStorage persistence remain unchanged. Implementation is section-by-section with testing after each step.

## Technical Context

**Language/Version**: TypeScript 5.x (strict mode)
**Primary Dependencies**: Next.js 15 (App Router), React 19, Zustand 5, Tailwind CSS 4
**Storage**: localStorage (browser-only, no server-side database)
**Testing**: Manual testing per section (connect to localhost target, select element, verify)
**Target Platform**: Web browser (Chrome, modern browsers)
**Project Type**: Web application (Next.js)
**Performance Goals**: Panel renders in <100ms, property edits preview within 1 frame
**Constraints**: Dark mode only, no external icon libraries, no new npm dependencies
**Scale/Scope**: 19 new files, 4 rewritten files, 4 deleted files

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Dark Mode Only | PASS | All new components use `var(--bg-tertiary)`, `var(--text-muted)`, `var(--accent)` |
| II. Iframe + Reverse Proxy | PASS | No changes to proxy, inspector, or postMessage layer |
| III. Localhost Only | PASS | No changes to URL validation or proxy routing |
| IV. Phase-Driven Implementation | PASS | This is Phase 3 work (Right Panel Properties Editor) — Phases 1-2 are complete |
| V. Zustand Single Store | PASS | No new store slices. All state read from existing slices. UI-local state via useState only |
| VI. Strategy Pattern | N/A | No drag or inspector behavior changes |
| VII. Changelog as Truth | PASS | All changes flow through `applyChange()` → `addStyleChange()` — unchanged |
| VIII. Bun Everywhere | PASS | No new scripts or build changes |
| IX. No Shell Exec | N/A | No CLI spawning in this feature |
| X. Singleton Message Listener | PASS | No changes to `usePostMessage` |
| XI. Middleware Scope | PASS | No middleware changes |
| XII. HMR Isolation | PASS | No proxy changes |

**Gate result**: ALL PASS. No violations to track.

## Project Structure

### Documentation (this feature)

```text
specs/002-design-tab-redesign/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Phase 0 research decisions
├── data-model.md        # Component interfaces and types
├── quickstart.md        # Development setup and testing guide
├── contracts/
│   └── component-contracts.md  # Component API contracts
├── checklists/
│   └── requirements.md  # Spec quality checklist
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
src/
├── components/
│   └── right-panel/
│       └── design/
│           ├── icons.tsx                    # SVG icon library (new)
│           ├── inputs/
│           │   ├── CompactInput.tsx         # Inline compact input (new)
│           │   ├── IconToggleGroup.tsx      # Icon button group (new)
│           │   ├── LinkedInputPair.tsx      # Linked 4-sided inputs (new)
│           │   ├── SectionHeader.tsx        # Enhanced collapsible (new)
│           │   └── ColorInput.tsx           # Color + variable picker (new)
│           ├── DesignCSSTabToggle.tsx       # Design | CSS toggle (new)
│           ├── CSSRawView.tsx              # Read-only CSS view (new)
│           ├── DesignPanel.tsx             # Orchestrator (rewrite)
│           ├── PositionSection.tsx         # Position controls (rewrite)
│           ├── LayoutSection.tsx           # Layout+size+spacing (rewrite)
│           ├── AppearanceSection.tsx       # Opacity+radius (new)
│           ├── TextSection.tsx            # Typography+color (new)
│           ├── BackgroundSection.tsx       # Background+gradient (new)
│           ├── GradientEditor.tsx         # Gradient stop editor (new)
│           ├── BorderSection.tsx          # Border controls (refactor)
│           ├── ShadowBlurSection.tsx      # Shadow+blur (new)
│           └── PropertiesSection.tsx      # Element metadata (new)
├── lib/
│   ├── constants.ts                       # CSS property groups (update)
│   ├── gradientParser.ts                  # Gradient parser (new)
│   └── shadowParser.ts                    # Shadow parser (new)
└── types/
    ├── gradient.ts                        # Gradient types (new)
    └── shadow.ts                          # Shadow types (new)
```

**Files deleted after migration**:
- `src/components/right-panel/design/PropertyInput.tsx` (replaced by CompactInput)

**Structure Decision**: Single web application structure. All new files placed within the existing `src/components/right-panel/design/` directory with a new `inputs/` subdirectory for foundation primitives. Parser utilities in `src/lib/`, types in `src/types/`.

## Implementation Phases

### Phase A: Foundation Primitives

Build the reusable input components that all sections depend on. After this phase, no sections are changed yet — only new components are created.

**Files created**:
1. `src/types/gradient.ts` — GradientStop, GradientData types
2. `src/types/shadow.ts` — ShadowData type
3. `src/components/right-panel/design/icons.tsx` — All SVG icon components
4. `src/components/right-panel/design/inputs/CompactInput.tsx` — Compact inline input
5. `src/components/right-panel/design/inputs/IconToggleGroup.tsx` — Icon toggle group
6. `src/components/right-panel/design/inputs/LinkedInputPair.tsx` — Linked 4-sided inputs
7. `src/components/right-panel/design/inputs/SectionHeader.tsx` — Enhanced collapsible section
8. `src/components/right-panel/design/inputs/ColorInput.tsx` — Color picker with variable support
9. `src/components/right-panel/design/DesignCSSTabToggle.tsx` — Design | CSS segmented control
10. `src/components/right-panel/design/CSSRawView.tsx` — Read-only CSS view

**Test gate**: `bun dev` starts without errors. New components can be imported without breaking existing sections.

---

### Phase B: Simple Section Rewrites

Rewrite existing sections to use foundation primitives. Each section is done independently and tested before moving to the next.

**B1. PositionSection rewrite**:
- Replace PropertyInput with CompactInput for X/Y/Z
- Replace position type dropdown with IconToggleGroup
- File: `src/components/right-panel/design/PositionSection.tsx` (rewrite)
- **Test**: Select element, change position type via icon toggle, edit top/left/z-index, verify live preview

**B2. AppearanceSection** (new):
- Opacity CompactInput (0-100%)
- Corner radius CompactInput with expand to 4 individual corners
- File: `src/components/right-panel/design/AppearanceSection.tsx` (new)
- **Test**: Select element, change opacity, change radius, expand to individual corners, verify preview

**B3. BorderSection refactor**:
- Replace PropertyInput with CompactInput
- Replace ColorPicker/VariableColorPicker with ColorInput
- Remove borderRadius (moved to AppearanceSection)
- File: `src/components/right-panel/design/BorderSection.tsx` (refactor)
- **Test**: Select element, change border width/style/color (including CSS variable tokens), verify preview

**B4. DesignPanel update — add new sections**:
- Add DesignCSSTabToggle between Element Info header and sections
- Add AppearanceSection to the section list
- Reorder sections: Position → Layout → Appearance → Border
- File: `src/components/right-panel/design/DesignPanel.tsx` (update)
- **Test**: Toggle Design/CSS view. Verify all sections render. Verify CSS view shows formatted styles.

---

### Phase C: Complex Sections

New sections that require new parsers or significant new logic.

**C1. Constants update**:
- Add new CSS property groups: shadow, appearance, flex-item, transform, filter
- Update ALL_EDITABLE_PROPERTIES
- File: `src/lib/constants.ts` (update)
- **Test**: `bun dev` starts. New properties appear in computedStyles when element selected.

**C2. LayoutSection major rewrite**:
- Merge display + flex/grid controls (existing)
- Add W/H dimensions as CompactInput pair
- Add padding/margin as LinkedInputPair
- Add clip content checkbox (overflow: hidden)
- Add border-box checkbox (boxSizing)
- File: `src/components/right-panel/design/LayoutSection.tsx` (rewrite)
- **Test**: Select element, change display type via icons, edit dimensions, toggle linked padding, verify preview

**C3. TextSection** (new):
- Font family searchable dropdown
- Weight/size row with CompactInput
- Color via ColorInput (CSS variable support)
- Line-height/letter-spacing row
- Text alignment via IconToggleGroup
- Text decoration/transform controls
- File: `src/components/right-panel/design/TextSection.tsx` (new)
- **Test**: Select text element, change font/weight/size/color/alignment, verify preview

**C4. Gradient parser + BackgroundSection**:
- Gradient parser: `src/lib/gradientParser.ts` (new)
- Gradient types: `src/types/gradient.ts` (already created in Phase A)
- Gradient editor: `src/components/right-panel/design/GradientEditor.tsx` (new)
- Background section: `src/components/right-panel/design/BackgroundSection.tsx` (new)
- **Test**: Select element, set solid bg color (with CSS var), switch to linear gradient, add/remove stops, verify CSS output

**C5. Shadow parser + ShadowBlurSection**:
- Shadow parser: `src/lib/shadowParser.ts` (new)
- Shadow types: `src/types/shadow.ts` (already created in Phase A)
- Shadow section: `src/components/right-panel/design/ShadowBlurSection.tsx` (new)
- **Test**: Select element, add shadow via +, edit X/Y/blur/spread/color, add second shadow, set filter blur, verify CSS

---

### Phase D: Orchestrator + Cleanup

**D1. PropertiesSection** (new):
- Read-only display of element tag, classes, attributes
- Collapsed by default
- File: `src/components/right-panel/design/PropertiesSection.tsx` (new)
- **Test**: Select element, expand Properties section, verify tag/classes/attributes display

**D2. DesignPanel final rewrite**:
- Render all sections in final order: Position → Layout → Appearance → Text → Background → Border → Shadow & Blur → Properties
- CSS tab renders CSSRawView
- All old sections removed from imports
- File: `src/components/right-panel/design/DesignPanel.tsx` (final rewrite)
- **Test**: Full regression — all sections render, all edits preview live, changes tracked

**D3. Cleanup**:
- Delete `PropertyInput.tsx` (no longer imported by any section)
- Verify no remaining imports of deleted component
- **Test**: `bun dev` starts, `bun run build` succeeds with no errors

## Complexity Tracking

No constitution violations. No complexity justifications needed.
