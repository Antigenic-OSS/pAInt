---
'@antigenic-oss/paint': minor
---

Add a globally installable `paint` CLI with `start`, `stop`, `restart`, `status`, and `logs` commands.

Set up automated release/version workflows with Changesets and GitHub Actions, and configure npm publishing metadata for public distribution.
